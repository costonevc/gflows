language: python
python:
  - "3.8"

deploy:
  provider: script
  skip_cleanup: true
  script: ssh -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP 'cd /root/gflows_git && git pull && ./deploy.sh'
  on:
    branch: main
